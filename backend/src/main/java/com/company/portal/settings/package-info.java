/**
 * System settings module (nested under administration for now):
 * strongly-typed accessors on the system_settings JSONB store.
 */
@org.springframework.modulith.ApplicationModule(
        displayName = "Settings"
)
package com.company.portal.settings;
