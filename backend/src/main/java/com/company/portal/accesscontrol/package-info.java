/**
 * Access control module: roles, permissions, RBAC evaluation and the
 * cached effective-permission set used by Spring Security authorities.
 */
@org.springframework.modulith.ApplicationModule(
        displayName = "Access Control"
)
package com.company.portal.accesscontrol;
